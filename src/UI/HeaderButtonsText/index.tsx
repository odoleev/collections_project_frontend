import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const HeaderButtonsText = styled(Typography)`
  @media (max-width: 550px) {
    line-height: 10px;
    font-size: 12px;
  }
`;
