import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const Clickable = styled(Typography)`
  cursor: pointer;
  padding: 1px 3px;
  border-radius: 5px;
  border: 1px solid lightgray;
  background-color: lightgray;
`;
